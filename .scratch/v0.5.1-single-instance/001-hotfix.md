# Prevent duplicate Companion instances

- [x] Capture the two live executable paths and rule out duplicate login items.
- [x] Stop the stray Debug instance on the current Mac.
- [x] Add and test a user-scoped lifetime lock before app initialization.
- [x] Reproduce by launching Release and Debug copies; verify one survivor.
- [ ] Publish and install v0.5.1 through Sparkle.
